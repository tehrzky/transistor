<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.y20k.transistor">

    <!-- Internet permission for streaming -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Google Drive sync permissions -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:allowBackup="true"
        android:name="org.y20k.transistor.TransistorApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="n">

        <!-- Main activity -->
        <activity
            android:name="org.y20k.transistor.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Settings activity -->
        <activity
            android:name="org.y20k.transistor.SettingsActivity"
            android:exported="false"
            android:parentActivityName="org.y20k.transistor.MainActivity" />

        <!-- NEW: Sync Settings activity -->
        <activity
            android:name="org.y20k.transistor.ui.SyncSettingsActivity"
            android:label="Sync Settings"
            android:exported="false" />

        <!-- Player service -->
        <service
            android:name="org.y20k.transistor.PlayerService"
            android:exported="false" />

    </application>
</manifest>
